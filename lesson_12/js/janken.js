function janken(myHand) {
  // じゃんけんの手を格納する変数(配列)の用意
  let handArray = ["グー", "チョキ", "パー"];
  // プレイヤーの手の決定
  // HTML側から引数として渡される
  // コンピュータの手の決定
  // ランダムに手を決める
  // 勝敗判定
  // if文
  // HTML側に勝敗を表示する
}
