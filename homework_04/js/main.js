function succession() {
  // request_busstop_time();
  const busstopdata = callApi();
  console.log(busstopdata);
  // downloadJson();

  // envdata = require("../.env");
  // console.log(envdata);
  // const now = timeread();
  // console.log(now);
  // console.log(comment);
}
